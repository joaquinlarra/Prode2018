<?
/*
if(count($this->data['top_menu_actions']) > 0)
{
	?>
	<ul class="nav nav-pills">
	<?
	foreach($this->data['top_menu_actions'] as $action_id => $action)
	{
		?>
        <li class="modelactions <?=$action['method'] == $current_method ? "active" : ""?>">
        <a href="<?= $action['url'] ? $action['url']."/".$parent_id : '#'.$this->class_name."/".$action['method']."/".$parent_id ?>" <?= $action['target'] ? "target='".$action['target']."'" : ""?> class="ajax-links-top-menu" ><?= $action['label']?></a>
        </li>
		<?
	}
	?>
	</ul>    
	<?
}*/
?>